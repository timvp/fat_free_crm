.remote
  = simple_form_for(@training_date, :html => one_submit_only(:training_date), :remote => true) do |f|
    = link_to_close edit_training_date_path(@training_date)
    -#= f.hidden_field :user_id

    = f.error_messages :object_name => t('training_date')

    = render "training_dates/top_section", :f => f, :edit => true
    -#= render "training_dates/extra",       :f => f, :edit => true
    -#= render "training_dates/web",         :f => f, :edit => true
    -#= render "fields/groups", :f => f, :edit => true
    -#= render "training_dates/permissions", :f => f, :edit => true

    .buttonbar
      = f.submit t(:save_training_date)
      #{t :or}
      = link_to_cancel edit_training_date_path(@training_date)