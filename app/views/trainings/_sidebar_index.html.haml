.filters#filters
  = render "shared/search"
  = hook(:index_training_sidebar_bottom, self)
  -#- Setting.contact_type.each do |key|
    .check_box{:style => "border-bottom: 1px silver dotted;"}
      %div{:style => "float:right;"}
        = @contact_type_total[key]
      = contact_type_state_checbox(key, @contact_type_total[key])
      = label_tag(key, t(key))
  -#.check_box
    %div{:style => "float:right;"}
      %b= @contact_type_total[:all]
    %b #{t :total_contacts}
