.remote
  = simple_form_for(@training_target, :html => one_submit_only(:training_target), :remote => true) do |f|
    = link_to_close edit_training_target_path(@training_target)
    -#= f.hidden_field :user_id

    = f.error_messages :object_name => t('training_target')

    = render "training_targets/top_section", :f => f, :edit => true
    -#= render "training_targets/extra",       :f => f, :edit => true
    -#= render "training_targets/web",         :f => f, :edit => true
    -#= render "fields/groups", :f => f, :edit => true
    -#= render "training_targets/permissions", :f => f, :edit => true

    .buttonbar
      = f.submit t(:save_training_target)
      #{t :or}
      = link_to_cancel edit_training_target_path(@training_target)