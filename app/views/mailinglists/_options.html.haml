%p
  = link_to_close options_mailinglists_path

  - current_sort_by = t("option_" + Mailinglist.sort_by_map.invert[@sort_by])


  = t(:sort_by, :models => t(:mailinglists_small), :field => link_to(current_sort_by, "#", :id => :sort_by)).html_safe
  = t(:show_per_page, :number => link_to(@per_page, "#", :id => :per_page), :models => t(:mailinglists_small), :fmt => 'regular').html_safe

%p

%script
  = render "shared/sort_by"
  = render "shared/per_page"